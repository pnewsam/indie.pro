import { Property } from "@/schemas/properties";

export const PropertyBrandForm = ({
  onSubmit,
  property_id,
  isLoading,
}: {
  onSubmit: (data: Property) => void;
  property_id: string;
  isLoading: boolean;
}) => {
  return <div>PropertyBrandForm</div>;
};
